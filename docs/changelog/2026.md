## Change Log & Release Notes

[Changelog Home](./index.md) | [2023-2025 Archive](./archive-2023-2025.md)

- Version 5.6.728 - 28 Jul 2026
  - Bumped package and assembly version to 5.6.728
  - Used the OKX API Version [2026-07-28](https://www.okx.com/docs-v5/log_en/#2026-07-28) entry to identify the RPI order-book, standard order, instruments, fee-rate, and public-trades surfaces, then compared every touched endpoint and channel with its current official contract
  - Added unsigned `GetRpiOrderBookAsync` support for `GET /api/v5/market/books-rpi`, including the documented `instId`/`sz` request, 1-400 depth validation, REST `seqId`, consolidated `[price,totalQty,nonRpiQty,count]` rows, cached-data semantics, and 20-per-2-second IP limit
  - Renamed public-trade source value `1` to `RetailPriceImprovementOrder`, retained the old ELP enum name as an obsolete binary-compatible alias, corrected the documented 500-row trade limit, and registered the current 100-per-2-second IP limit
  - Synchronized the private `orders` WebSocket response model with the complete current field table, including tag, USD notionals, per-update fill P&amp;L/fees, option execution prices, execution role, amend source/result/request ID, latest price, and update code/message; added typed `amendSource=6` RPI spacing-round coverage
  - Verified that current place/batch/amend REST and WebSocket requests, public/private instruments, fee-rate fields, and `books-rpi` WebSocket support were already aligned; added the missing 5-per-2-second User ID fee-rate limiter without duplicating existing models
  - Retained documented ELP aliases for transition compatibility through October 31, 2026 and recorded retired error `54045` in the reference docs without inventing a static error catalog that the wrapper does not have
  - Added documentation-derived REST and WebSocket fixtures plus unsigned request, validation, response, current-name mapping, and compatibility regression coverage without placing orders or sending private requests to OKX

- Version 5.6.724 - 24 Jul 2026
  - Bumped package and assembly version to 5.6.724
  - Used the OKX API Version [2026-07-24](https://www.okx.com/docs-v5/log_en/#2026-07-24) entry to identify `POST /api/v5/trade/order`, then compared the complete request and response tables with the current official endpoint contract
  - Removed `speedBump` from the positional single-order API and rejected it before sending from the shared request-model overload, preventing callers from assuming that OKX's silently ignored parameter still delays an Event Contract order
  - Preserved `speedBump` for REST batch placement and Amend contracts where the current official endpoint tables still document it, instead of deleting the shared enum or request property
  - Omitted default `posSide=net` and WebSocket-only `instIdCode` from REST placement payloads, serialized option `pxUsd`/`pxVol` values as documented strings, enforced the one-of `px`/`pxUsd`/`pxVol` rule locally, and added the missing place-response `tag`
  - Added local request and fixture regression coverage without sending private orders to OKX
  - Completed the scheduled retrospective review of the July 3, 7, 14, 16, and 23 changes against their current models, endpoint tables, release notes, and deterministic regression suite; no additional corrective change was required beyond resolving the stale `speedBump` discrepancy in this release

- Version 5.6.723 - 23 Jul 2026
  - Bumped package and assembly version to 5.6.723
  - Used the OKX API Version [2026-07-23](https://www.okx.com/docs-v5/log_en/#2026-07-23) entry to identify the two GLP performance endpoints plus trigger-chase Place/Amend/Details/List/History/WebSocket surfaces, then compared every touched endpoint with its current official contract
  - Added signed GLP today and newest-first historical performance queries under `api.Account`, including typed SPOT/PERP/FUT_NTO programs, account grouping, daily/MTD qualification metrics, nullable FUT_NTO categories, validation, and separate 5-per-2-second User ID limits
  - Added FUTURES/SWAP trigger-to-chase placement and pre-trigger amendment through nested `advChaseParams`, preserved standalone chase root fields as a distinct contract, and exposed spawned chase algo IDs through `subAlgoIdList`
  - Corrected `advanceOrdType` response parsing from the unrelated price-type enum to `OkxAlgoTriggerOrderType`, and synchronized current REST/WebSocket order fields including latest order ID, order tag, WebSocket notional/amendment metadata, and place-response tag/client ID
  - Synchronized the currently documented `smart_iceberg` Place/List/History contract because it is present on the same touched endpoint tables, including split count, aggressiveness, optional instant/price/RSI triggers, and response parsing
  - Added the documented combined `conditional,oco` pending/history filter, enforced history state-or-ID and supported instrument filters, and rejected invalid Algo orders channel subscription shapes before opening a socket
  - Added documentation-derived GLP, trigger-chase, and smart-iceberg fixtures plus signed local request, validation, response, identifier-transition, and throttling coverage without sending private orders or account requests to OKX

- Version 5.6.716 - 16 Jul 2026
  - Bumped package and assembly version to 5.6.716
  - Used the OKX API Version [2026-07-16](https://www.okx.com/docs-v5/log_en/#2026-07-16) entry to identify the public/private Instruments REST endpoints and Instruments WebSocket channel, then compared all three surfaces with their current official response tables
  - Verified that the shared instrument model already covers every currently documented field across the three touched surfaces, including the existing `preMktSwTime` property and `pre_market`/`xperp` rule mappings, so no duplicate product model was introduced
  - Documented that Pre-market X-Perps are queried as `FUTURES`, clarified their `pre_market` to `xperp` conversion semantics, and removed obsolete SWAP-only wording from shared instrument-state documentation
  - Added documentation-derived public/private REST and WebSocket regression fixtures for both the pre-market phase and a converted X-Perp with a populated switch timestamp

- Version 5.6.714 - 14 Jul 2026
  - Bumped package and assembly version to 5.6.714
  - Used the OKX API Version [2026-07-14](https://www.okx.com/docs-v5/log_en/#2026-07-14) entry to identify the decommissioned Stable Rewards quote, trade, and subscribe/redeem-history endpoints, then verified their removal against the current official API reference
  - Audited the complete wrapper surface and confirmed that none of the three removed endpoint paths or corresponding client methods had been exposed, so no executable code removal or misleading compatibility shim was required
  - Documented the intentional absence of those methods and directed USDG and other stablecoin trading to the existing standard order-book surface under `api.Trade`

- Version 5.6.707 - 07 Jul 2026
  - Bumped package and assembly version to 5.6.707
  - Used the OKX API Version [2026-07-07](https://www.okx.com/docs-v5/log_en/#2026-07-07) entry to identify Event Contract series/markets and contract cool-off order rejection, then compared each touched surface with its current official contract
  - Verified that `monthly`, `hit`, `between`, `capStrike`, and `hitDir` were already implemented by the earlier current-contract synchronization; corrected the manual HIT/BETWEEN fixtures and expanded REST/WebSocket contract assertions instead of duplicating those models
  - Applied the independent 10-per-2-second IP limits to Event Contract series and markets, rejected invalid series filters and null market-channel callbacks locally, and documented that the WebSocket channel sends no initial snapshot
  - Synchronized the currently documented Place/Batch/Amend REST and WebSocket request contract with `ordType=rpi`, `rpiTakerAccess`, and `rpiPxRound`, enforced the 1-to-20 batch size, and retained the officially supported ELP aliases as deprecated transition members through October 31, 2026
  - Restored the previously claimed but missing `slippagePct` place-order field across REST and WebSocket, including the official 0-to-0.05 range and four-decimal-place validation before a request is sent
  - Documented server-side cool-off semantics and added REST/WebSocket regression coverage proving that error `54094` is preserved without attempting to predict account cool-off state client-side; recorded the official `speedBump` changelog-versus-endpoint-table discrepancy without guessing which side OKX will correct

- Version 5.6.703 - 03 Jul 2026
  - Bumped package and assembly version to 5.6.703
  - Used the OKX API Version [2026-07-03](https://www.okx.com/docs-v5/log_en/#2026-07-03) entry to identify the MM instrument classification and OKUSD limits/subscribe/redeem endpoints, then synchronized each touched endpoint with its current official contract
  - Added the unsigned SPOT/SWAP Market Maker Program classification query with typed `A`, `B-Crypto`, and `B-TradFi` results and its 5-per-2-second IP limit; also accepted the verified production-only `Type ` prefixes without changing canonical mappings
  - Added a signed `Financial.Okusd` client for master-account-shared limits, idempotent 1:1 USDT subscriptions, and fast or standard OKUSD redemptions
  - Enforced the documented minimum amount, 8-decimal-place, client-order-ID, and redemption-type request constraints before sending a financial request
  - Applied separate User ID limits for OKUSD limits, subscription, and redemption and retained OKUSD error codes `51763`–`51774` through the standard numeric server-error path
  - Added official-payload fixtures plus local model, request, authentication, validation, and throttling coverage without sending private financial requests to OKX

- Version 5.6.623 - 23 Jun 2026
  - Bumped package and assembly version to 5.6.623
  - Used the OKX API Version [2026-06-23](https://www.okx.com/docs-v5/log_en/#2026-06-23) entry to identify the JSON order book channels, then synchronized the touched WebSocket channel with its current official contract
  - Marked `checksum` obsolete while retaining its fixed-zero payload field for `books`, `books-l2-tbt`, and `books50-l2-tbt`; documented `seqId`/`prevSeqId` continuity, keepalive, and maintenance-reset rules
  - Added current `books-rpi` support and temporary deprecated `books-elp` compatibility, including the RPI-specific `nonRpiQty` order-book value without breaking the legacy property
  - Updated channel timing and VIP4 authentication descriptions, and rejected null callbacks, empty/invalid instrument IDs, and unknown channel enum values before opening a socket
  - Added current documentation-derived snapshot, incremental, and RPI fixtures plus local mapping, authentication, model, compatibility, sequence, and validation coverage without opening a live WebSocket connection

- Version 5.6.611 - 11 Jun 2026
  - Bumped package and assembly version to 5.6.611
  - Used the OKX API Version [2026-06-11](https://www.okx.com/docs-v5/log_en/#2026-06-11) entry to identify the service-upgrade notice and bill-data application endpoint, then synchronized both with their current official contracts
  - Added a root `ServiceUpgradeNotice` event for code `64008` across public, private, and business WebSocket connections while leaving reconnect and state restoration under application control
  - Isolated service-notice subscriber failures so one application callback cannot suppress other registered callbacks or interrupt socket message handling
  - Applied method-specific User ID limits for `POST` and `GET /api/v5/account/bills-history-archive`, corrected endpoint descriptions, and validated documented request shapes
  - Added official-payload fixtures plus local serialization, response, notification-routing, validation, and throttling coverage without opening a live WebSocket or sending private requests to OKX

- Version 5.6.609 - 09 Jun 2026
  - Bumped package and assembly version to 5.6.609
  - Used the OKX API Version [2026-06-09](https://www.okx.com/docs-v5/log_en/#2026-06-09) entry to identify the Security Fund REST endpoint and ADL warning WebSocket channel, then synchronized both with their current official contracts
  - Enforced the documented `ccy` versus `instFamily` query shapes and rejected unknown security-fund type values before sending a request
  - Marked the `platform_revenue`, `adl`, and scheduled-for-removal REST/ADL fields obsolete while retaining empty-string-compatible parsing
  - Updated the ADL warning contract to warning/ADL-only one-second pushes, removed synthetic `instId` projection, and retained the historical `normal` state only for payload compatibility
  - Recorded and covered the confirmed production divergence where `regular_update` is rejected as a request filter but remains present in current unfiltered/`all` responses
  - Added current official fixtures, a post-change production snapshot, and local validation/contract coverage without opening a live WebSocket connection

- Version 5.6.605 - 05 Jun 2026
  - Bumped package and assembly version to 5.6.605
  - Used the OKX API Version [2026-06-05](https://www.okx.com/docs-v5/log_en/#2026-06-05) entry to identify asset bills history, then synchronized both shared asset-bills query surfaces with their current official contracts
  - Added typed Copper, Komainu, and SCB `thirdPartyType` filtering for custody bill types `284`-`289`
  - Restricted `pagingType` serialization to asset bills history, validated its documented values, and rejected attempts to send it to asset bills details
  - Corrected the DCD broker rebate and SOL staking reward bill type mappings and added the currently documented auto-lend, stablecoin-earn, Cloud Exchange, and OKUSD bill types
  - Added local query, validation, mapping, and response coverage without sending private funding requests to OKX

- Version 5.6.602 - 02 Jun 2026
  - Bumped package and assembly version to 5.6.602
  - Used the OKX API Version [2026-06-02](https://www.okx.com/docs-v5/log_en/#2026-06-02) entry to identify the Instruments channel, then synchronized the shared public/private REST and WebSocket instrument contract with the current official documentation
  - Documented the `SPACEX-USDT-SWAP` to `SPCX-USDT-SWAP` rename, including changed `uly`, `instFamily`, and `ctValCcy` values and the stable `instIdCode`
  - Preserved the official `expired` → `rebase` → `post_only` → `live` update sequence without silently rewriting user-supplied instrument IDs
  - Corrected instrument fee `groupId` from a numeric value to the documented string identifier and refreshed current group mappings and X-Perp applicability descriptions
  - Restricted Instruments channel subscriptions to the six currently documented instrument types and rejected empty subscription lists before opening a connection
  - Added local rename-sequence, model, and validation coverage without opening a live WebSocket connection

- Version 5.6.520 - 20 May 2026
  - Bumped package and assembly version to 5.6.520
  - Used the OKX API Version [2026-05-20](https://www.okx.com/docs-v5/log_en/#2026-05-20) entry to identify the global REST domain and fee-rate surface, then synchronized them with the current official contracts
  - Changed the default Global and Demo REST address to OKX's recommended `https://openapi.okx.com` domain while leaving WebSocket endpoints unchanged; retained explicit `www.okx.com` configuration compatibility
  - Redirected the ceased AWS compatibility address to active Global endpoints and marked it obsolete instead of leaving nonfunctional domains exposed
  - Added current `rpiMaker`, transitional `elpMaker`, an effective fallback, and the complete deprecated root/fiat fee-rate response fields
  - Enforced the documented instrument-type and mutually exclusive fee-rate selectors locally, including EVENTS support
  - Added local domain, validation, and response-contract coverage without sending private account requests to OKX

- Version 5.6.519 - 19 May 2026
  - Bumped package and assembly version to 5.6.519
  - Used the OKX API Version [2026-05-19](https://www.okx.com/docs-v5/log_en/#2026-05-19) entry to identify Event Contracts, instruments, and tick-band surfaces, then synchronized them with the current official contracts
  - Removed authentication from Event Contract series, events, and markets REST requests and updated live-test configuration to use the public client
  - Added current series frequencies and settlement methods, plus market `capStrike` and `hitDir` response coverage
  - Added EVENTS support to the general instrument tick-band method while preserving the OPTION-specific compatibility method
  - Expanded public/private/WebSocket instrument parsing with coin-denominated platform OI limits, RPI permission and spacing, price-limit percentages, deprecated auction/category fields, and current X-Perp metadata
  - Enforced current conditional `seriesId`, `instFamily`, instrument-type, event-state, and tick-band request rules locally
  - Added local contract and unsigned-request coverage without sending private account requests to OKX

- Version 5.6.515 - 15 May 2026
  - Bumped package and assembly version to 5.6.515
  - Used the OKX API Version [2026-05-15](https://www.okx.com/docs-v5/log_en/#2026-05-15) entry to identify Move Positions and Copy Trading endpoints, then synchronized the currently documented endpoint contracts
  - Added 16- and 18-character lead-trader code validation across copy settings and all active public analytics methods, plus typed 7/30/90/365-day performance windows
  - Corrected reversed cross/isolated copy-margin serialization, corrected statistics currency parsing to a string currency code, and added the current copy-settings `tag` response field
  - Aligned copy-setting conditional requirements and nullable `instType` handling, and documented that historical private analytics and multiple-leverage links are no longer active in the current official documentation
  - Updated Move Positions to the current VIP6 contract, current TradeFi equity perpetual/XPerp support, and local validation for accounts, client IDs, and the 30-leg limit
  - Added local contract coverage without sending private copy-setting or position-transfer requests to OKX

- Version 5.6.514 - 14 May 2026
  - Bumped package and assembly version to 5.6.514
  - Used the OKX API Version [2026-05-14](https://www.okx.com/docs-v5/log_en/#2026-05-14) entry to identify the affected FD Broker endpoints, then synchronized both endpoints with their current official contracts
  - Implemented FD broker rebate download-link retrieval with typed broker selection, date formatting, conditional range validation, and complete download metadata
  - Implemented FD broker rebate eligibility lookup with typed status values, including `type=4` for MSAs that are not entitled to broker rebate
  - Documented the current downloaded CSV schema, including the added `uid` and optional `clOrdId` attribution columns
  - Added local contract coverage without sending private broker requests to OKX

- Version 5.6.511 - 11 May 2026
  - Bumped package and assembly version to 5.6.511
  - Used the OKX API Version [2026-05-11](https://www.okx.com/docs-v5/log_en/#2026-05-11) entry to identify the affected Block Trading REST and WebSocket surfaces, then synchronized them with their current official contracts
  - Clarified `filled` and maker-only `traded_away` RFQ semantics across the REST model, RFQ query, RFQ channel, and public API reference
  - Documented that private structure trade pushes go only to the taker and executing maker, and recorded the public/private channel cross-reference rules for normal and Group RFQs
  - Hardened private and public Group RFQ structure trade parsing so documented empty parent `blockTdId` and leg `tradeId` values deserialize as null while per-account execution identifiers remain available
  - Added manual contract fixtures for maker `traded_away` state and private/public Group RFQ structure channel payloads

- Version 5.6.508 - 08 May 2026
  - Bumped package and assembly version to 5.6.508
  - Used the OKX API Version [2026-05-08](https://www.okx.com/docs-v5/log_en/#2026-05-08) entry to identify the affected Affiliate endpoints, then synchronized all six endpoints with their current official contracts
  - Added Affiliate performance summary, paginated invitee, link, co-inviter link, and sub-affiliate methods with typed period, filter, sort, KYC, link, and pagination parameters
  - Added root-level `totalPage` preservation and current response coverage for period/lifetime metrics, rolling 24-hour commission, compliance, link assessment, and channel status fields
  - Corrected invitee detail parsing from the obsolete `inviteeLv` JSON key to current `inviteeLevel`, and added `wdAmt`, `totalVol`, and optional `volPeriod`
  - Added request validation and local behavior tests for all affected endpoints without sending private Affiliate requests to OKX

- Version 5.6.507 - 07 May 2026
  - Bumped package and assembly version to 5.6.507
  - Synced `POST /api/v5/account/demo-adjust-balance` with the current official endpoint documentation, using the OKX API Version [2026-05-07](https://www.okx.com/docs-v5/log_en/#2026-05-07) entry as the discovery point
  - Added typed demo balance increase/reduce requests and responses, including string-encoded amounts, balances, and daily increase quota counts
  - Enforced demo-only usage and the documented supported currencies, non-negative amounts, duplicate-currency rule, and per-request increase limits before sending the request
  - Added local client-behavior coverage for request serialization, demo headers, response parsing, reduce behavior, validation, and documented top-level error handling without mutating a live demo account

- Version 5.6.506 - 06 May 2026
  - Bumped package and assembly version to 5.6.506
  - Synced with the OKX API Version [2026-05-06](https://www.okx.com/docs-v5/log_en/#2026-05-06)
  - Added `post_only` instrument state support for private instruments, public instruments, and the instruments WebSocket channel
  - Synced with the OKX API Version [2026-04-28](https://www.okx.com/docs-v5/log_en/#2026-04-28)
  - Added delta-neutral precheck coverage for the `risk_unit_type` unmatched information type

- Version 5.6.427 - 27 Apr 2026
  - Fixed Block Trading request serialization for `CreateRfqAsync` account allocations and `CreateQuoteAsync` client quote IDs, and aligned create-quote responses with the documented `Quote` model
  - Expanded Block Trading RFQ, quote, trade, and public trade models with current documented metadata such as `tag`, `flowType`, `groupId`, `isSuccessful`, and `errorCode`
  - Made `GET /api/v5/rfq/public-trades` a true public request, hardened empty-string block trade identifiers for parent-level public group RFQ responses, and added Block Trading contract plus public integration coverage
  - Added `Financial.EthStaking.CancelRedeemAsync` for `POST /api/v5/finance/staking-defi/eth/cancel-redeem`
  - Fixed ETH and SOL staking purchase/redeem helpers so documented `code=0, data=[]` responses now report success instead of `false`
  - Extended on-chain earn response coverage with `tag`, completed history state `3`, `redeemedTime`, `realizedEarnings`, and nullable empty-string timestamp/amount handling
  - Replaced on-chain earn purchase/redeem/cancel helpers with full `OkxFinancialOrderReference` responses so `ordId` and echoed `tag` are both preserved
  - Added Financial Product contract and local client-behavior tests for staking, on-chain earn, and deprecated `redemptAmt` balance coverage
  - Expanded Funding Account coverage with the 13 missing fiat deposit/withdrawal and buy/sell endpoints, including fiat payment methods, fiat order history/detail flows, and buy/sell quotes and trade history
  - Fixed funding withdrawal receiver serialization so `rcvrStreetName` is emitted with the correct JSON key and aligned convert quote parsing so `rfqSzCcy` stays a currency code string
  - Added detailed funding status and convert-currency metadata models, required the documented `ccy` filter on deposit-address requests, and exposed the optional `ccy` filter on currencies requests
  - Hardened Funding Account response parsing for live `addrEx` attachments and empty-string numeric/timestamp fields in currency metadata
  - Added `Dca` REST client coverage for the documented DCA Trading surface, including place/amend/stop/order-history flows, manual buy, reinvestment/take-profit settings, position details, cycle history, and margin adjustment endpoints
  - Fixed Grid Trading request parity by sending `mktClose` on contract-grid close requests, serializing `triggerParams` as structured payloads, and adding the documented `copy-order-algo` and `amend-algo-basic-param` endpoints
  - Fixed Signal Bot order-details requests to use GET query parameters and added docs-aligned `CreateSignalAsync` and `GetSignalsAsync` aliases alongside the existing channel-centric methods
  - Expanded Recurring Buy with the six missing management endpoints, corrected its private order-updates channel to use the authenticated `/ws/v5/business` socket, and aligned recurring-order models with current docs fields and states
  - Added Order Book Trading contract and local client-behavior test coverage for DCA, Grid, Signal Bot, and Recurring Buy request/response parity
  - Added Funding Account contract, local client-behavior, and safe live integration coverage, with rate-limited live endpoints now skipping instead of failing the suite on OKX `50011` responses
  - Fixed Public Data parity for security fund, interest-rate-loan-quota, discount-rate-interest-free-quota, economic calendar, liquidation orders, ADL warning, and live open-interest payloads
  - Added `GetInsuranceFundsAsync` and `GetInterestRateLoanQuotaAsync`, kept the legacy single-item security fund helper for compatibility, and aligned economic-calendar REST and WebSocket flows with OKX's authenticated business-socket requirement
  - Hardened Public Data models for empty-string numeric/timestamp fields, added current documented deprecated fields, corrected liquidation size and ADL `maxBalTs` mappings, and updated public open-interest parsing so `oi` supports fractional live values
  - Added manual docs fixtures plus committed live production snapshots for discount info, interest-rate-loan-quota, security fund, underlying, option tick bands, open interest, index components, and authenticated economic calendar data, with new contract, client-behavior, and public integration coverage
  - Aligned Spread Trading REST filters with current docs by making mass-cancel `sprdId` optional, adding `instType` and `instFamily` support to order-archive queries, and validating spread-only order state/type filters before requests are sent
  - Implemented the Spread Trading WebSocket surface for place/amend/cancel/mass-cancel, private `sprd-orders` and `sprd-trades`, and public business-socket subscriptions for spread order books, public trades, tickers, and candlesticks
  - Hardened Spread Trading models for documented empty-string fields such as `expTime`, `fillPx`, `tradeId`, `fillPnl`, `fee`, and `szCont`, added spread `tag` coverage, and expanded cancel-source handling with the latest `44` code
  - Added manual docs fixtures plus committed live production spread snapshots for spread list, order book, ticker, public trades, and candlesticks, with new spread contract, socket, client-behavior, and live public integration coverage
  - Aligned Sub Account request contracts by sending `CreateSubAccountAsync` as a documented JSON body, validating that only the OKX-supported sub-account types (`1`, `5`, `12`) are accepted there, making `SetPermissionOfTransferOutAsync` reflect the documented optional flag default, and making custody sub-account filtering optional
  - Fixed Sub Account response fidelity by returning all rows from `GetSubAccountFundingBalancesAsync`, hardening maximum-withdrawal parsing for documented empty-string extended fields, correcting `canTransOut` to a boolean response, and expanding trading-balance coverage with current OKX delta, collateral, copy-trading, and auto-lend fields
  - Added Sub Account manual contract fixtures, local client-behavior tests, and a safe live read-only integration test for sub-account list, trading balance, funding balance, and maximum withdrawal flows
  - Fixed Trading Account parity by sending `position-builder-graph` `mmrConfig` as the documented object payload, returning every row from `GetPositionTiersAsync`, and handling empty-string `posType` values as nullable
  - Added the missing `Account.SetTradingConfigAsync` and `Account.PrecheckSetDeltaNeutralAsync` endpoints with docs-aligned strategy response models and delta-neutral precheck coverage for `deltaLever`, `ordList`, and `posList`
  - Updated Trading Account private WebSocket subscriptions so `account` and `positions` now send stringified `extraParams.updateInterval`, `liquidation-warning` accepts optional `instFamily` and `instId`, and `account-greeks` accepts optional `ccy`
  - Added Trading Account manual contract fixtures, local client-behavior tests, socket contract coverage for private subscription payloads, and a safe live read-only delta-neutral precheck integration test
  - Aligned Trading Statistics with current OKX docs by validating endpoint-specific period values, enforcing `SPOT`/`CONTRACTS` only for taker-volume requests, and adding a `TradingStatistics` alias on the root REST client for discoverability
  - Corrected `GET /api/v5/rubik/stat/option/open-interest-volume-expiry` expiry handling so `expTime` is exposed as a `YYYYMMdd` date token with parsed `ExpiryDate`/`ExpiryDateTime` helpers instead of being treated as Unix milliseconds
  - Added full Trading Statistics docs-shape fixtures plus committed live production snapshots for all 15 public endpoints, alongside new Rubik contract, client-behavior, and public integration coverage
  - Hardened the live fixture capture script to save raw OKX JSON payloads instead of PowerShell re-serialized objects, and aligned the existing trade account-rate-limit live integration test with rate-limit skip behavior

- Version 5.6.426 - 26 Apr 2026
  - Bumped package and assembly version to 5.6.426
  - Synced with the OKX API Version [2026-02-05](https://www.okx.com/docs-v5/log_en/#2026-02-05)
  - Added `longPosRemainingQuota` and `shortPosRemainingQuota` to `OkxPublicInstrument` for private instruments responses
  - Synced with the OKX API Version [2026-02-12](https://www.okx.com/docs-v5/log_en/#2026-02-12)
  - Added `instCategory` to `OkxPublicInstrument`
  - Added `OkxPublicInstrumentCategory` enum with current `Crypto` mapping
  - Synced with the OKX API Version [2026-02-27](https://www.okx.com/docs-v5/log_en/#2026-02-27)
  - Added `lendingRate` to public borrow history responses
  - Synced with the OKX API Version [2026-03-02](https://www.okx.com/docs-v5/log_en/#2026-03-02)
  - Expanded `OkxPublicInstrumentCategory` mappings to match the instruments endpoint documentation (`Stocks`, `Commodities`, `Forex`, `Bonds`; empty string continues to deserialize as `null`)
  - Synced with the OKX API Version [2026-03-04](https://www.okx.com/docs-v5/log_en/#2026-03-04)
  - Added `rebase` instrument state and `rebase_contract` instrument rule type for instruments REST responses and channel payloads
  - Synced with the OKX API Version [2026-03-10](https://www.okx.com/docs-v5/log_en/#2026-03-10)
  - Added `subCode` parsing for place/amend order REST and WebSocket operation responses, and surfaced it through REST order error details
  - Updated one-click repay v2 method documentation to reflect support for SPOT, Multi-currency margin, and Portfolio margin modes
  - Synced with the OKX API Version [2026-03-13](https://www.okx.com/docs-v5/log_en/#2026-03-13)
  - Added `Financial.DualInvestment` with eight new REST endpoints for currency pairs, products, quote requests, trade confirmation, redeem quote requests, redemption, order status, and order history
  - Added dual investment order state models and robust `GetProductsAsync` payload handling for both the documented array response and the current live `data.products` wrapper seen in production
  - Added manual contract fixtures for all eight dual investment endpoints and safe production integration coverage for the read-only GET endpoints
  - Synced with the OKX API Version [2026-03-18](https://www.okx.com/docs-v5/log_en/#2026-03-18)
  - Updated estimated delivery/exercise/settlement price documentation from a 1-hour window to the current 30-minute calculation window
  - Corrected the public delivery/exercise history method description and added manual REST/WS contract fixtures for estimated price payloads
  - Synced with the OKX API Version [2026-03-24](https://www.okx.com/docs-v5/log_en/#2026-03-24)
  - Added `Account.GetBillTypesAsync` for `GET /api/v5/account/subtypes`
  - Added optional multi-value bill type filters to `ApplyBillDataAsync` and `GetBillDataAsync` for the since-2021 bills archive endpoints
  - Added account bill type contract coverage and a safe private integration test for filtered bill type lookups
  - Synced with the OKX API Version [2026-03-26](https://www.okx.com/docs-v5/log_en/#2026-03-26)
  - Updated WebSocket place order payloads to require `instIdCode` and stop sending delisted `instId` on `WS / Place order` and `WS / Place multiple orders`
  - Added contract coverage for WebSocket place order request serialization and missing `instIdCode` validation
  - Synced with the OKX API Version [2026-03-31](https://www.okx.com/docs-v5/log_en/#2026-03-31)
  - Added `xperp` instrument rule type support and documented the new `this_five_years` and `next_five_years` futures alias values
  - Expanded funding rate REST and WebSocket documentation to reflect X-Perps futures support and the live `ANY` aggregate funding-rate behavior
  - Added manual and live contract coverage for `GET /api/v5/public/funding-rate` and `GET /api/v5/public/funding-rate-history`, plus production integration tests for X-Perps funding data
  - Synced with the OKX API Version [2026-04-07](https://www.okx.com/docs-v5/log_en/#2026-04-07)
  - Updated WebSocket amend and cancel order payloads to require `instIdCode` and stop sending deprecated `instId` on `WS / Amend order`, `WS / Amend multiple orders`, `WS / Cancel order`, and `WS / Cancel multiple orders`
  - Fixed WebSocket batch cancel to use the correct `batch-cancel-orders` operation and corrected batch amend/cancel response matching for socket query handling
  - Added request serialization and socket query-response contract coverage for WebSocket amend/cancel order flows
  - Synced with the OKX API Version [2026-04-08](https://www.okx.com/docs-v5/log_en/#2026-04-08)
  - Hardened `GET /api/v5/trade/account-rate-limit` parsing for VIP4 downgrade responses where `fillRatio`, `mainFillRatio`, and `nextAccRateLimit` may be returned as empty strings
  - Added manual contract coverage and a safe private integration test for trade account rate limit responses
  - Synced with the OKX API Version [2026-04-10](https://www.okx.com/docs-v5/log_en/#2026-04-10)
  - Implemented `Financial.FlexibleLoan` with flexible loan borrow currencies, collateral assets, maximum loan, maximum collateral redeem amount, collateral adjustment, loan info, loan history, and accrued interest endpoints
  - Added `ordId` support across the flexible loan endpoint family and surfaced the new `ordId` response field on flexible loan info payloads
  - Expanded historical market data module coverage with `4` (400-level orderbook), `5` (5000-level orderbook), and `11` (borrowing rate), and aligned the docs/comments with the current endpoint rules for `ANY` and 20-day/20-month ranges
  - Hardened historical market data parsing for the current `dateTs` response example while keeping compatibility with the documented `dataTs` field name
  - Added `rate`, `redemptDays`, and `minAmt` to staking product info responses and aligned SOL staking product info with its object-shaped response payload
  - Added manual contract fixtures plus live integration coverage for historical market data borrowing-rate snapshots and ETH/SOL staking product info
  - Synced with the OKX API Version [2026-04-13](https://www.okx.com/docs-v5/log_en/#2026-04-13)
  - Added attached trailing stop `callbackRatio`, `callbackSpread`, and `activePx` fields to trade and algo `attachAlgoOrds` request, amend, and response models
  - Aligned nested algo attached-order request/response models with the current endpoint docs for attached TP/SL trigger ratios
  - Fixed trade attached algo request/response serialization for `slOrdPx` and `amendPxOnTriggerType`, which the new contract tests exposed while adding trailing stop coverage
  - Added manual contract coverage for attached trailing stop serialization and response parsing without placing live orders
  - Synced with the OKX API Version [2026-04-15](https://www.okx.com/docs-v5/log_en/#2026-04-15)
  - Added authenticated Event Contracts public data support with `GetEventContractSeriesAsync`, `GetEventContractEventsAsync`, `GetEventContractMarketsAsync`, and `SubscribeToEventContractMarketsAsync`
  - Added `seriesId` support to public and private instruments requests, expanded instrument state coverage with `expired`, `test`, and `settling`, and enabled `EVENTS` on public open-interest and mark-price requests
  - Added Event Contracts order/account support with `speedBump`, `outcome`, `settle` fee rate parsing, and bills subtypes `410`-`415`
  - Added manual and live Event Contracts contract coverage, authenticated public integration tests, and signed live fixture capture for event-contract series, events, markets, and event mark/open-interest snapshots
  - Hardened event mark-price parsing so `markPx` now tolerates live OKX responses that return an empty string before the price is available
  - Added `OKX.Api.Tests` with manual edge-case fixtures and committed live OKX public response snapshots
  - Added optional live integration tests with local `.env` configuration kept out of git
  - Added a live fixture capture script and aligned simple earn borrow history amount parsing with current public snapshots
  - Updated `instIdCode` handling to `long` after validating demo trading public instrument responses that exceed 32-bit integer range
  - Split committed live snapshots into `Production` and `Demo` fixture sets to track environment-specific API behavior separately
  - Added demo availability coverage for public borrow history, which currently returns `50038` in demo trading
  - Fixed top-level OKX `code/msg` handling for generic REST responses so list and model endpoints no longer report API errors as successful calls

- Version 5.6.216 - 16 Feb 2026
  - Fixed issue [GetInstrumentsAsync fails #93](https://github.com/burakoner/OKX.Api/issues/93)

- Version 5.6.210 - 10 Feb 2026
  - Added OkxPublicSocketClient.SubscribeToOptionTradesAsync() method
  - Added OkxPublicSocketClient.SubscribeToCallAuctionsAsync() method
  - Added OkxPublicSocketClient.SubscribeToLiquidationOrdersAsync() method
  - Added OkxPublicSocketClient.SubscribeToAdlWarningsAsync() method
  - Added OkxPublicSocketClient.SubscribeToEconomicCalendarEventsAsync() method

- Version 5.6.209 - 09 Feb 2026
  - Updated to ApiSharp 4.5.0
  - Synced with the OKX Latest API Version [2026-01-21](https://www.okx.com/docs-v5/log_en/#2026-01-21)
  - Enhance type safety, serialization, and ELP/convert support
  - Add useLargeOrderConvert and isElpTakerAccess params to funding/trade REST clients
  - Add ElpMakerPermission to instrument model with new enum
  - Introduce Boolean/Decimal as string JSON converters
  - Refactor algo order models for type safety (decimals, bools)
  - Add overload for PlaceOrderAsync with request object
  - Remove old attached algo request records
  - Improve XML docs and code clarity throughout
  - Refactor funding/sub-account APIs, add new endpoints/models
  - Renamed funding methods for clarity and consistency
  - Added GetBillsHistoryAsync and replaced OkxFundingExchangeList with OkxFundingExchange
  - Updated usages in Program.cs and bumped ApiSharp to 4.5.0
  - Implemented sub-account management methods: create, API key ops, query, delete
  - Added OkxSubAccountSummary and OkxSubAccountApiKeyCredential models
  - Cleaned up comments, TODOs, and improved documentation throughout

* Version 5.6.111 - 11 Jan 2026
  - Synced with the OKX API Version [2026-01-07](https://www.okx.com/docs-v5/log_en/#2026-01-07)
  - OKX API v5.6.111: Fee, order, instrument, and model updates
  - Refactor account fee rates: add groupId/FeeGroup, remove ruleType and fiat/USDC/USDT-specific fields
  - Instrument model: add groupId, upcoming changes, improve position limit docs
  - Order place/amend/cancel: support instIdCode, mark instId obsolete, add PriceAmendType
  - Algo orders: add advanceOrdType (FOK/IOC), trigger ratios, order kind, split TP/SL, cost-price SL
  - Trade model: add source (OkxTradeOrderSource), Enhanced Liquidity Program order type
  - Staking: add Cancelled status, OrderId to history
  - Improve docs, update namespaces, minor .csproj reordering
  - Add OkxAlgoTriggerOrderType.cs, OkxTradeOrderSource.cs enums
  - Update models and endpoints for latest OKX API changes
  - Refactor account fee rates: add groupId, FeeGroup, remove ruleType and fiat/USDC/USDT-specific fee properties
  - Add groupId and upcoming changes to instrument model
  - Support instIdCode for order place/amend/cancel; mark instId as obsolete
  - Enhance TP/SL handling: add trigger ratios, order kind, split TP/SL options
  - Add Cancelled status to staking; add OrderId to staking history
  - Improve documentation and update namespaces
  - Minor .csproj property reordering for clarity

* Version 5.6.110 - 10 Jan 2026
  - Synced with the OKX API Version [2025-11-13](https://www.okx.com/docs-v5/log_en/#2025-11-13)
  - Add delta neutral strategy support to OKX account models
  - Enhanced account, position, and loan quota models to support delta neutral strategy features.
  - Added new properties for delta, delta leverage, and hedged positions.
  - Introduced enums for strategy type and delta neutral status, and added support for custom loan quota configurations.
  - Made withdrawal-related fields nullable for better data handling.


